package com.example.hanaparal.data.repository

import com.example.hanaparal.data.remote.FirebaseAuthSource

class AuthRepository(private val authSource: FirebaseAuthSource) {
    // Repository implementation for Auth
}
